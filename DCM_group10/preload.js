//const { contextBridge, ipcRenderer } = require('electron')

/*contextBridge.exposeInMainWorld('eAPI', {
  bcryptHash: (plaintextPassword) => ipcRenderer.invoke('bcryptHash', plaintextPassword),
  bcryptCompare: (plaintextPassword, hash) => ipcRenderer.invoke('bcryptCompare', plaintextPassword, hash)
})*/
